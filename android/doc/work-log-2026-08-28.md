# 2026-08-28 工作日志（恢复版）

> 原始 work-log 于 2026-08-31 10:32 文件截断事故中丢失；本文件依据档案重建：
> CONTEXT.md「问题反馈与建议模块」段 + 会话转录（`2026-08-28T05-33-59Z`）。

- **问题反馈与建议模块（完成，API 留空）**：入口 About 页「问题和建议」→ `app/.../ui/FeedbackActivity`
  （独立 Activity，纯 UI + 本地闭环）。
  - 页面流：表单（类型胶囊/描述 500 字/附件≤6张≤5MB/日志勾选/联系方式）→ 成功页（插画+查看按钮）
    → 列表（状态点：灰已提交/橙处理中/蓝已回复/灰已关闭）→ 详情（编号 yyyyMMddHHmm + 气泡对话 + 三态底栏）
    → 关闭确认弹窗；空态/加载态/图片全屏查看器齐。
  - 数据：`feedback/FeedbackRecord` + `FeedbackStore`（SharedPreferences JSON）；`FeedbackApi` URL 留空 TODO(api)。
  - 设计基准：Figma `429:20679` 起全部反馈页。
  - 相关字符串/资源全部并入（08-31 已从 APK 恢复）；`AboutActivity` 反馈行 → `startActivity(FeedbackActivity)`。
- **审查评论头像**：FunctionPanelController/CalcFPC/ImpressFPC/ImpressCommentPickerController 四组 Host 接口
  新增 `getCommentAuthorAvatarUri()`（LOActivity 读 `AI_PROFILE_AVATAR_URI` 偏好）。
- 构建：17:30 debug APK（collabora-office-20260828-1730）——该产物成为 08-31 截断事故的恢复快照。
