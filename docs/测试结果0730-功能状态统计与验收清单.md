# Office移动端测试结果0730 — 功能状态统计与验收清单

> 基于 0730 版 APK 测试报告（手机端 118 条 + Pad 端 36 条），对照当前 `android` 分支源码分析。
> 分析日期：2026-08-04 | 测试人：夏斐（手机）、朱广强（Pad）

---

## 一、本次修复内容（8 项）

### P0 — 已修复

| # | Bug | 修改文件 | 改动说明 |
|---|-----|---------|---------|
| 1 | 粗体/斜体/下划线/删除线无选中状态反馈 | `LOActivity.java`、`FunctionPanelController.java`、`CalcFunctionPanelController.java`、`ImpressFunctionPanelController.java`、`BottomToolbarController.java` | `fetchCurrentFormattingAsync` 新增 bold/italic/underline/strikethrough 查询，各面板按钮选中时蓝色高亮 |

### P1 — 已修复

| # | Bug | 修改文件 | 改动说明 |
|---|-----|---------|---------|
| 2 | Writer 插入表格无行列选择 | `FunctionPanelController.java` | 新增 `showInsertTableDialog()` 带 +/- steppers，替代硬编码 2×2 |
| 3 | 另存为缺 Calc 格式 | `LOActivity.java` | `showSaveAsFormatDialog()` 新增 `mIsCalcDocument` 分支：ODS/XLSX/XLS |
| 4 | PPT 格式切换无效 | 代码已具备 | `FORMAT_COMMANDS` 已含 `.uno:AttributePageSize?PaperFormat:short=N`，旧版本问题 |
| 5 | PPT 幻灯片放映无入口 | `ImpressFunctionPanelController.java`、`LOActivity.java` | 文件 Tab 新增"幻灯片放映"按钮，JS 调 `slideShow._startFullscreenSlideshow()` |
| 6 | 插入图片无拍照选项 | `LOActivity.java` | `launchLocalImagePickerForInsert` 改为弹窗选择"拍照/从相册"，新增 camera capture 流程 |

### P2 — 已修复

| # | Bug | 修改文件 | 改动说明 |
|---|-----|---------|---------|
| 7 | 清理缓存是 stub | `LibreOfficeUIActivity.java`、`LOActivity.java` | 实现 `clearAppCache()`/`clearDocumentCache()`：递归清理 cacheDir + extCacheDir，显示释放空间 |
| 8 | PPT 背景选项是 stub | 代码已具备 | `BACKGROUND_COMMANDS` + `createBackgroundActions()` 已含颜色选择器、图片背景等，旧版本问题 |

---

## 二、验收 Checklist

> 打 APK 后按以下清单逐项验证。每项标 ✅ 或 ❌，如 ❌ 请附 logcat 截图。

### Checklist A — 本次修复项验证

| # | 验证项 | 操作步骤 | 预期结果 |
|---|--------|---------|---------|
| A1 | **粗体选中状态** | Word 编辑模式 → 选中文本 → 点"字符" → 点粗体 → 移动光标到粗体文字 | 粗体按钮蓝色高亮；离开粗体文字时恢复正常 |
| A2 | **斜体选中状态** | 同 A1，测试斜体 | 斜体按钮蓝色高亮 |
| A3 | **下划线/删除线选中状态** | 同 A1 | 按钮高亮正确反映状态 |
| A4 | **Calc 粗体等选中状态** | Excel 编辑模式 → 功能 → 字符工具 → 选中有粗体的单元格 | 粗体按钮蓝色高亮 |
| A5 | **Writer 插入表格** | Word 编辑模式 → 插入 → 表格 | 弹出行列选择对话框，默认 2×2，可 +/- 调整（1~20），点"插入"后表格按选择行列插入 |
| A6 | **Excel 另存为格式** | Excel → 文件 → 另存为 | 格式选项为 ODS / XLSX / XLS（不再是 ODT/DOCX） |
| A7 | **PPT 幻灯片放映** | PPT 编辑模式 → 文件 → 幻灯片放映 | 启动全屏幻灯片放映 |
| A8 | **插入图片弹窗** | Word 编辑模式 → 插入图片 | 弹出选择框："拍照" / "从相册选择" |
| A9 | **拍照插入图片** | A8 选"拍照" → 拍照 → 确认 | 拍摄的照片插入到文档中 |
| A10 | **清理缓存（首页）** | 首页侧边栏 → 清理缓存 | 弹出确认对话框 → 点"清理" → 显示释放空间 |
| A11 | **清理缓存（文档页）** | 文档页设置 → 清理缓存 | 同 A10 |
| A12 | **PPT 背景颜色** | PPT 编辑模式 → 幻灯片 → 背景 → 颜色 | 弹出颜色选择器，选择后背景变色 |
| A13 | **PPT 格式切换** | PPT 编辑模式 → 幻灯片 → 格式 → 选 A3 | 幻灯片尺寸切换为 A3 |

### Checklist B — 功能完整性验证（之前报障但代码已具备的功能）

| # | 验证项 | 前置条件 | 预期结果 |
|---|--------|---------|---------|
| B1 | AI 模型配置 | 设置 → AI 模型配置 | 可输入 URL / API Key / 模型名称并保存 |
| B2 | AI 面板对话 | 配置好模型 → 打开文档 → AI 助手 → 聊天 | 可输入并流式显示回复 |
| B3 | AI doc_qa | AI 助手 → 文档问答 | 首轮自动注入全文，可对文档提问 |
| B4 | AI 续写 | 选中文本 → AI 功能 → 续写 | 流式预览 → 插入文档 |
| B5 | AI 翻译 | 选中文本 → AI 功能 → 翻译 | 选择源/目标语言 → 翻译结果 |
| B6 | AI 扩写/缩写/重写 | 选中文本 → 对应功能 | 弹出要求输入框 → 生成结果 → 自动粘贴 |
| B7 | AI 排版 | 选中文本 → AI 功能 → 排版 | 选择排版类型 → 预览 → 插入 |
| B8 | AI 大纲 | AI 功能 → 大纲 | 选择大纲类型 → 生成 → 可复制 |
| B9 | AI 条件格式 | Excel 编辑 → AI 功能 → 条件格式 | 输入需求 → 生成 → 应用到单元格 |
| B10 | AI 公式 | Excel 编辑 → AI 功能 → 公式 | 输入需求 → 生成公式 → 插入/复制 |
| B11 | AI 数据处理 | Excel 编辑 → AI 功能 → 数据处理 | 输入需求 → 执行 → 完成 |
| B12 | AI 批量格式处理 | AI 功能 → 批量格式处理 | 勾选规则 → 执行（纯本地处理，不需模型） |
| B13 | OCR 拍照识别 | AI 功能 → 文字提取 | 相册/拍照 → 识别文字 → 可应用到文档 |
| B14 | AI 图片生成 | 配置图片生成模型 → AI 功能 → AI 图片 | 输入描述 → 生成图片 |
| B15 | AI 生成 Word | 首页新建 Word → 开 AI 开关 → 输入主题 | 自动生成文档内容 |
| B16 | AI 生成 Excel | 首页新建 Excel → 开 AI 开关 → 输入描述 | 自动生成表格内容 |
| B17 | AI 生成 PPT | 首页新建 PPT → 开 AI 开关 → 输入主题 | 生成多页真实 .pptx 幻灯片 |
| B18 | 查找替换 | 任意文档 → 功能 → 查找替换 | 底部弹出查找替换面板 |
| B19 | 选区菜单操作 | 选中文本 → 弹出选区菜单 | 显示复制/剪切/AI 等操作 |
| B20 | 水印 | Word → 布局 → 水印 | 输入水印文字 → 应用 |
| B21 | 页面设置 | Word → 布局 → 页边距/纸张/方向 | 选择后页面参数变化 |
| B22 | 插入批注 | 任意文档 → 插入 → 批注 | 批注插入成功 |
| B23 | 插入超链接 | PPT/Excel → 插入超链接 | 输入链接 → 插入 |
| B24 | Excel 重命名工作表 | Excel 长按 sheet tab → 重命名 | 输入新名称 → 生效 |
| B25 | 标签页管理 | 工具栏 → 标签页 | 显示已打开/最近关闭列表 |

### Checklist C — 回归验证（确保修改未引入新问题）

| # | 验证项 | 预期结果 |
|---|--------|---------|
| C1 | 文档打开/关闭 | 正常打开 docx/xlsx/pptx，关闭不崩溃 |
| C2 | 编辑模式切换 | 预览 ↔ 编辑模式切换正常 |
| C3 | 键盘弹出 | 键盘弹出时工具栏 compact 模式正常 |
| C4 | 息屏恢复 | 息屏 15s → 亮屏 → 文档正常显示 |
| C5 | 保存 | 保存功能正常 |
| C6 | 分享 | 分享功能正常（非 tmp 格式） |

---

## 三、总览统计（全量 154 条）

| 状态 | 手机 | Pad | 合计 | 占比 |
|------|------|-----|------|------|
| ✅ 功能已实现（旧版本/配置问题） | 72 | 22 | 94 | 61% |
| ⚠️ 部分实现 → 本次修复 | 8 | 0 | 8 | 5% |
| ⚠️ UI/设计待对齐 | 17 | 7 | 24 | 16% |
| ❌ 未实现/需产品确认 | 12 | 4 | 16 | 10% |
| 🎨 UI 设计调整类 | 9 | 3 | 12 | 8% |

---

## 四、仍需关注的未修复项

> 以下项目未在本次修复中处理，需产品决策或后续迭代。

### 需产品确认

| 问题 | 说明 |
|------|------|
| 应用名称 | 当前 "orange office"，测试期望 "AI Office" |
| 扩写/缩写/重写要求页面 | 测试认为不应有，当前有意设计为有 |
| doc_qa 默认选中全文 | 测试认为不应默认选中，当前有意设计为首轮注入全文 |
| Pad AI 助手位置 | 测试期望右侧，当前底部 BottomSheet |

### 待后续开发

| 问题 | 优先级 | 说明 |
|------|--------|------|
| 模型供应商下拉选择 | P2 | 当前 EditText，需改为 Spinner |
| 模型名称从 API 获取 | P2 | 需调 `/v1/models` 端点 |
| 模型连接测试 | P2 | 需添加 test 按钮 |
| 问题和建议模块 | P3 | 完全不存在 |
| 模型供应商下拉表 | P2 | 当前 EditText |
| Excel 大表格单元格难选中 | P3 | 需排查 Collabora tile 渲染 |
| PPT 更多字段 | P3 | `toastTodo` 占位 |
| 频繁崩溃 | P0 | 建议接入 Crashlytics |
| 幻灯片放映图标 | P2 | 当前借用打印图标，需换专用图标 |

---

## 五、修改文件清单

| 文件 | 修改类型 |
|------|---------|
| `LOActivity.java` | P0: 格式化查询增加 bold/italic/strike; P1: Calc 另存为、幻灯片放映、拍照插入; P2: 清理缓存 |
| `FunctionPanelController.java` | P0: FormattingCallback 接口扩展; P1: 插入表格行列选择器 |
| `CalcFunctionPanelController.java` | P0: bold/italic 按钮选中状态 |
| `ImpressFunctionPanelController.java` | P0: bold/italic 按钮选中状态; P1: 幻灯片放映入口 |
| `BottomToolbarController.java` | P0: 快捷面板 bold/italic 按钮选中状态 |
| `LibreOfficeUIActivity.java` | P2: 清理缓存功能实现 |
